/**
 * AudioPlayer is integrated into PlayerContext via HTMLAudioElement ref.
 * This component exists as a hook point for future audio visualizations.
 */
const AudioPlayer = () => null;

export default AudioPlayer;
